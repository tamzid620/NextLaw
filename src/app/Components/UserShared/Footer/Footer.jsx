import { open_sans } from '@/fonts/fonts';
import React from 'react';

const Footer = () => {
    return (
        <footer className= {` ${open_sans.className} bg-[#124547] text-white `}>

        <div>
            <h1>Footer</h1>
        </div>
        </footer>
    );
};

export default Footer;